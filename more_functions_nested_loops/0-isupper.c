#include "main.h"
/**
 * _supper - Write a function that checks for uppercase character
 * @c: find c
 * Return: 0
*/
int _isupper(int c)
{
	if

	(c >= 'A' && c <= 'Z')
	return (1);
	else
	return (0);
}
