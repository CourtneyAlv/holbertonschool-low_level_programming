#include "main.h"
/**
 * _strlen - Write a function that returns the length of a string.
 * @s: length of a string
 * Return: 0
*/
int _strlen(char *s)
{
	int length = 0;

	while
	(*s++)
	length++;
	return (length);
}
