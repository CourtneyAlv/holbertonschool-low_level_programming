#include <stdio.h>
/**
 * main - hexadecimal
 * Return: 0
*/
int main(void)
{
	int h = 48;

	while
	(h < 58)
{
	putchar(h);
	(h++);
}
	h = 'a';
	while
	(h <= 'f')
{
	putchar(h);
	(h++);
}
	putchar('\n');
	return (0);
}
