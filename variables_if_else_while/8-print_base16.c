#include <stdio.h>
/**
 * main - hexadecimal
 * Return: 0
*/
int main(void)
{
	int h;

	for
	(h = 48; h <= 58; h++)
{
	putchar(h);
}
	for
	(h = 'a'; h <= 'f'; h++)
{
	putchar(h);
}
	putchar('\n');
	return (0);
}
